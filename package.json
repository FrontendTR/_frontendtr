{
  "name": "frontendtr",
  "version": "1.0.0",
  "description": "Frontend Türkiye Web",
  "main": "tailwind.config.js",
  "author": "Yusuf Ulusoy",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "npx tailwindcss -i ./tailwind.css -o ./styles.css --watch",
    "build": "npx tailwindcss -i ./tailwind.css -o ./styles.css --minify"
  },
  "devDependencies": {
    "tailwindcss": "^3.0.2"
  }
}
